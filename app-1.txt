(()=>{"use strict";const t={portrait:{width:1080,height:1350,label:"1080 × 1350"},square:{width:1080,height:1080,label:"1080 × 1080"},story:{width:1080,height:1920,label:"1080 × 1920"},landscape:{width:1200,height:628,label:"1200 × 628"}},o={format:"portrait",shader:"dither",colorA:"#ff6a3d",colorB:"#5528ff",colorC:"#00e5ff",scale:1.15,complexity:.62,contrast:1.18,grain:.18,motion:.38,warp:.45,glow:.58,angle:0,seed:3.3,paused:!1,text:"Design should feel alive.",font:"Arial",weight:"700",fontSize:112,lineHeight:.94,tracking:-3,textWidth:78,textColor:"#ffffff",shadowColor:"#000000",shadow:.18,align:"center",verticalAlign:"middle",x:50,y:50,rotation:0,zoom:.58},e={...o},n=document.getElementById("displayCanvas"),a=document.getElementById("shaderCanvas"),i=n.getContext("2d",{alpha:!1}),l=a.getContext("webgl",{preserveDrawingBuffer:!0,antialias:!1});l||alert("WebGL is required to render procedural backgrounds in this prototype.");const r=document.getElementById("canvasWrap"),c=document.getElementById("selectionFrame"),u=document.getElementById("dimensionsLabel");let s=performance.now(),m=0,d=1,f=0,g={x:0,y:0,width:0,height:0},p=!1,v=null,h=null;const x="\n    precision highp float;\n    varying vec2 v_uv;\n    uniform vec2 u_resolution;\n    uniform float u_time;\n    uniform vec3 u_colorA;\n    uniform vec3 u_colorB;\n    uniform vec3 u_colorC;\n    uniform float u_scale;\n    uniform float u_complexity;\n    uniform float u_contrast;\n    uniform float u_grain;\n    uniform float u_warp;\n    uniform float u_glow;\n    uniform float u_angle;\n    uniform float u_seed;\n\n    float hash(vec2 p) {\n      p += vec2(u_seed * 17.17, u_seed * 31.31);\n      p = fract(p * vec2(123.34, 456.21));\n      p += dot(p, p + 45.32);\n      return fract(p.x * p.y);\n    }\n    float noise(vec2 p) {\n      vec2 i = floor(p); vec2 f = fract(p);\n      f = f*f*(3.0-2.0*f);\n      return mix(mix(hash(i), hash(i+vec2(1.,0.)), f.x), mix(hash(i+vec2(0.,1.)), hash(i+vec2(1.,1.)), f.x), f.y);\n    }\n    float fbm(vec2 p) {\n      float value = 0.0;\n      float amplitude = 0.5;\n      for (int i = 0; i < 5; i++) {\n        value += amplitude * noise(p);\n        p = p * 2.03 + vec2(7.1, 3.7);\n        amplitude *= 0.5;\n      }\n      return value;\n    }\n    mat2 rot(float a) { float c=cos(a), s=sin(a); return mat2(c,-s,s,c); }\n    vec3 triColor(float x) {\n      x = clamp(x, 0.0, 1.0);\n      return x < 0.5 ? mix(u_colorA, u_colorB, x * 2.0) : mix(u_colorB, u_colorC, (x - 0.5) * 2.0);\n    }\n    vec3 finishColor(vec3 col, vec2 uv) {\n      col = (col - 0.5) * u_contrast + 0.5;\n      float g = hash(uv * u_resolution + fract(u_time) * 37.0) - 0.5;\n      col += g * u_grain;\n      float vignette = 1.0 - smoothstep(0.18, 1.15, length(uv - 0.5));\n      col *= mix(0.88, 1.04, vignette);\n      return clamp(col, 0.0, 1.0);\n    }\n  ",_={dither:x+"\n      void main() {\n        vec2 uv = v_uv;\n        vec2 p = (uv - 0.5) * vec2(u_resolution.x/u_resolution.y,1.0) * u_scale;\n        p *= rot(radians(u_angle));\n        float n = fbm(p * (2.2 + u_complexity*6.0) + vec2(u_time*.08,-u_time*.06));\n        float wave = sin(p.x*4.2 + sin(p.y*5.1+u_time*.25)*mix(.4,2.8,u_warp)) * .5 + .5;\n        float m = smoothstep(.08,.92, mix(wave,n,u_complexity*.82));\n        vec3 col = triColor(m);\n        float edge = 1.0 - abs(m * 2.0 - 1.0);\n        col += u_colorC * pow(edge, 8.0) * u_glow * .35;\n        float cells = mix(3.0, 15.0, u_complexity);\n        vec2 grid = floor(gl_FragCoord.xy / cells);\n        float d = hash(grid);\n        float luminance = dot(col, vec3(.299,.587,.114));\n        float bit = step(d, luminance);\n        col = mix(col * .24, col * 1.2 + .08, bit * .62);\n        gl_FragColor = vec4(finishColor(col, uv),1.0);\n      }\n    ",fluid:x+"\n      void main() {\n        vec2 uv = v_uv;\n        vec2 p = (uv-.5) * vec2(u_resolution.x/u_resolution.y,1.0) * u_scale * 2.1;\n        p *= rot(radians(u_angle));\n        float t = u_time*.19;\n        vec2 q = p;\n        q += mix(.08,.58,u_warp)*sin(vec2(p.y*2.1+t,p.x*2.4-t*.8));\n        q += mix(.04,.34,u_warp)*sin(vec2(q.y*4.0-t*1.3,q.x*3.4+t));\n        float f = sin(q.x*2.0 + sin(q.y*2.5+t))*0.5+0.5;\n        float n = fbm(q*(1.4+u_complexity*3.1)+t);\n        float m = smoothstep(.04,.96,mix(f,n,u_complexity*.62));\n        vec3 col = triColor(m);\n        float ribbon = pow(1.0-abs(fract(m*3.0+t*.08)-.5)*2.0, 7.0);\n        col += u_colorC * ribbon * u_glow * .26;\n        gl_FragColor = vec4(finishColor(col,uv),1.0);\n      }\n    ",metal:x+"\n      void main() {\n        vec2 uv = v_uv;\n        vec2 p = (uv-.5) * vec2(u_resolution.x/u_resolution.y,1.0);\n        p *= rot(radians(u_angle) + .28*sin(u_time*.12));\n        p += vec2((fbm(p*3.0+u_time*.05)-.5) * u_warp * .35);\n        float r = length(p) * (3.0 + u_scale*2.0);\n        float a = atan(p.y,p.x);\n        float folds = sin(r*6.0 - u_time*.5 + sin(a*5.0)*u_complexity*3.0);\n        folds += .55*sin(r*11.0 + a*3.0 + u_time*.24);\n        float ridge = pow(abs(folds*.5), mix(1.0,4.0,u_complexity));\n        float shine = smoothstep(.42,.98,ridge);\n        vec3 base = mix(u_colorA*.12,u_colorB*.42,.5+.5*sin(a+r*.25));\n        base = mix(base, u_colorC*.48, .18+.18*sin(a*3.0));\n        vec3 silver = vec3(.14 + shine*.98);\n        vec3 col = mix(base,silver,.54+shine*.38);\n        col += pow(max(0.0,1.0-length(p-vec2(-.12,.2))),8.0)*u_glow;\n        gl_FragColor = vec4(finishColor(col,uv),1.0);\n      }\n    ",aurora:x+"\n      void main() {\n        vec2 uv = v_uv;\n        vec2 p = (uv-.5) * vec2(u_resolution.x/u_resolution.y,1.0) * u_scale;\n        p *= rot(radians(u_angle));\n        float t = u_time*.13;\n        float n1 = fbm(p*2.5+vec2(t,-t));\n        float n2 = noise(p*7.0-vec2(t*.4,t*.8));\n        float curtain = sin((p.x+n1*mix(.2,1.05,u_warp))*5.0 + p.y*2.0 + t*2.0)*.5+.5;\n        curtain = pow(curtain, mix(1.2,4.4,u_complexity));\n        float glow = exp(-abs(p.y + .18*sin(p.x*2.2+t) + (n2-.5)*.38*u_warp)*3.2);\n        float m = clamp(curtain*.66+glow*.78,0.0,1.0);\n        vec3 col = mix(u_colorB*.10,triColor(m),m);\n        col += mix(u_colorC,u_colorA,curtain) * glow*u_glow*.62;\n        col += vec3(.03,.08,.15)*(1.0-uv.y);\n        gl_FragColor = vec4(finishColor(col,uv),1.0);\n      }\n    ",mesh:x+"\n      void main() {\n