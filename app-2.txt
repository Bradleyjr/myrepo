       vec2 uv = v_uv;\n        vec2 p = (uv-.5) * vec2(u_resolution.x/u_resolution.y,1.0) * u_scale;\n        p *= rot(radians(u_angle));\n        float t = u_time*.10;\n        vec2 c1 = vec2(-.44 + .12*sin(t+u_seed), .28 + .13*cos(t*.8));\n        vec2 c2 = vec2(.38 + .14*cos(t*.7+2.0), .12 + .18*sin(t+1.4));\n        vec2 c3 = vec2(.02 + .22*sin(t*.55+4.0), -.42 + .12*cos(t*.9));\n        float wobble = (fbm(p*2.6+t)-.5)*u_warp*.5;\n        float a = exp(-length(p-c1+vec2(wobble))*mix(2.2,5.2,u_complexity));\n        float b = exp(-length(p-c2-vec2(wobble))*mix(2.0,4.8,u_complexity));\n        float c = exp(-length(p-c3+vec2(wobble*.5))*mix(2.3,5.6,u_complexity));\n        vec3 col = (u_colorA*a + u_colorB*b + u_colorC*c) / max(.22,a+b+c);\n        float bloom = pow(clamp(a+b+c,0.0,1.0),2.0);\n        col = mix(vec3(.035),col,clamp((a+b+c)*1.25,0.0,1.0));\n        col += vec3(1.0)*bloom*u_glow*.22;\n        gl_FragColor = vec4(finishColor(col,uv),1.0);\n      }\n    ",halftone:x+"\n      void main() {\n        vec2 uv = v_uv;\n        vec2 p = (uv-.5)*vec2(u_resolution.x/u_resolution.y,1.0)*u_scale;\n        p *= rot(radians(u_angle));\n        float t=u_time*.18;\n        float field = sin(p.x*3.2 + sin(p.y*4.0+t)*mix(.4,2.4,u_warp))*.5+.5;\n        field = mix(field,fbm(p*(2.0+u_complexity*4.0)+t),u_complexity*.62);\n        vec3 source = triColor(field);\n        float cellSize = mix(5.0,22.0,1.0-u_complexity);\n        vec2 cell = fract(gl_FragCoord.xy/cellSize)-.5;\n        float lum = dot(source,vec3(.299,.587,.114));\n        float radius = mix(.10,.48,lum);\n        float dotMask = 1.0-smoothstep(radius,radius+.075,length(cell));\n        vec3 paper = mix(u_colorB*.12,u_colorA*.42,field);\n        vec3 col = mix(paper*.22,source*1.18,dotMask);\n        col += u_colorC * pow(dotMask,4.0)*u_glow*.18;\n        gl_FragColor=vec4(finishColor(col,uv),1.0);\n      }\n    ",hologram:x+"\n      void main() {\n        vec2 uv=v_uv;\n        vec2 p=(uv-.5)*vec2(u_resolution.x/u_resolution.y,1.0)*u_scale;\n        p*=rot(radians(u_angle));\n        float t=u_time*.12;\n        float n=fbm(p*(2.2+u_complexity*3.0)+vec2(t,-t*.7));\n        p += (n-.5)*u_warp*.42;\n        float spectrum=sin((p.x+p.y*.65)*8.0+n*6.0+t*2.0)*.5+.5;\n        vec3 rainbow=.5+.5*cos(6.28318*(spectrum+vec3(.00,.33,.67)));\n        vec3 tint=triColor(fract(spectrum*.82+n*.4));\n        float foil=pow(.5+.5*sin(p.x*18.0+p.y*5.0+n*9.0),mix(1.0,5.0,u_complexity));\n        float sweep=pow(max(0.0,1.0-abs(p.x+p.y*.35-sin(t)*.25)*3.2),7.0);\n        vec3 col=mix(tint,rainbow,.58);\n        col=mix(col,col*.28,foil*.42);\n        col+=vec3(1.0)*(sweep*u_glow*.9+foil*.12);\n        gl_FragColor=vec4(finishColor(col,uv),1.0);\n      }\n    ",topography:x+"\n      void main() {\n        vec2 uv=v_uv;\n        vec2 p=(uv-.5)*vec2(u_resolution.x/u_resolution.y,1.0)*u_scale;\n        p*=rot(radians(u_angle));\n        float t=u_time*.06;\n        float n=fbm(p*(1.7+u_complexity*2.2)+vec2(t,-t));\n        n+=sin(p.x*2.0+p.y*1.4+t)*u_warp*.16;\n        float levels=mix(7.0,24.0,u_complexity);\n        float contour=abs(fract(n*levels)-.5);\n        float line=1.0-smoothstep(.035,.11,contour);\n        float major=1.0-smoothstep(.025,.065,abs(fract(n*levels*.2)-.5));\n        vec3 base=mix(u_colorA*.08,u_colorB*.32,n);\n        vec3 col=mix(base,u_colorC,line*(.48+u_glow*.45));\n        col+=u_colorA*major*u_glow*.3;\n        gl_FragColor=vec4(finishColor(col,uv),1.0);\n      }\n    ",glass:x+"\n      void main() {\n        vec2 uv=v_uv;\n        vec2 p=(uv-.5)*vec2(u_resolution.x/u_resolution.y,1.0)*u_scale;\n        p*=rot(radians(u_angle));\n        float t=u_time*.14;\n        float nx=fbm(p*3.0+vec2(t,0.0));\n        float ny=fbm(p*3.0+vec2(0.0,-t)+8.0);\n        vec2 refract=vec2(nx-.5,ny-.5)*u_warp*.72;\n        vec2 q=p+refract;\n        float caustic=abs(sin(q.x*7.0+sin(q.y*5.0+t))*cos(q.y*8.0+sin(q.x*4.0-t)));\n        caustic=pow(caustic,mix(1.1,4.2,u_complexity));\n        float m=fbm(q*2.0+t*.2);\n        vec3 col=triColor(m);\n        col=mix(col,col*.22,.42*(1.0-caustic));\n        col+=vec3(1.0)*caustic*u_glow*.78;\n        col+=u_colorC*pow(caustic,5.0)*.28;\n        gl_FragColor=vec4(finishColor(col,uv),1.0);\n      }\n    ",plasma:x+"\n      void main() {\n        vec2 uv=v_uv;\n        vec2 p=(uv-.5)*vec2(u_resolution.x/u_resolution.y,1.0)*u_scale;\n        p*=rot(radians(u_angle));\n        float t=u_time*.22;\n        float n=fbm(p*(2.0+u_complexity*3.0)+t*.15);\n        p += vec2(sin(p.y*3.0+t),cos(p.x*3.4-t))*u_warp*.16;\n        float r=length(p)+n*u_warp*.28;\n        float a=atan(p.y,p.x);\n        float rings=sin(r*mix(14.0,38.0,u_complexity)-t*2.0+sin(a*5.0)*2.0)*.5+.5;\n        float m=smoothstep(.08,.92,rings);\n        vec3 col=triColor(fract(m*.72+n*.62));\n        float hot=pow(1.0-abs(rings-.5)*2.0,6.0);\n        col+=vec3(1.0)*hot*u_glow*.52;\n        col*=.72+.48*m;\n        gl_FragColor=vec4(finishColor(col,uv),1.0);\n      }\n    ",ink:x+"\n      void main() {\n        vec2 uv=v_uv;\n        vec2 p=(uv-.5)*vec2(u_resolution.x/u_resolution.y,1.0)*u_scale;\n        p*=rot(radians(u_angle));\n        float t=u_time*.09;\n        vec2 q=vec2(fbm(p*2.1+t),fbm(p*2.1+vec2(5.2,-t)));\n        vec2 r=vec2(fbm(p*3.2+q*mix(1.0,4.0,u_warp)),fbm(p*3.2-q*mix(1.0,3.4,u_warp)+8.0));\n        float m=fbm(p*(1.4+u_complexity*2.2)+r*3.0);\n        float vein=pow(1.0-abs(sin((m+r.x-r.y)*18.0)) ,mix(2.0,8.0,u_complexity));\n        vec3 col=triColor(m);\n        col=mix(col,col*.12,vein*.64);\n        col+=u_colorC*pow(1.0-vein,8.0)*u_glow*.24;\n        gl_FragColor=vec4(finishColor(col,uv),1.0);\n      }\n    ",prism:x+"\n      void main() {\n        vec2 uv=v_uv;\n        vec2 p=(uv-.5)*vec2(u_resolution.x/u_resolution.y,1.0)*u_scale;\n        p*=rot(radians(u_angle));\n        float t=u_time*.10;\n        float n=(fbm(p*3.0+t)-.5)*u_warp*.44;\n        float axis=p.x+n+sin(p.y*2.0+t)*u_warp*.08;\n        float w=mix(.035,.16,1.0-u_complexity);\n        float b1=1.0-smoothstep(w,w*2.2,abs(axis+.34));\n        float b2=1.0-smoothstep(w,w*2.2,abs(axis));\n        float b3=1.0-smoothstep(w,w*2.2,abs(axis-.34));\n        float flare=1.0-smoothstep(.0,.65,abs(axis));\n        vec3 col=mix(vec3(.025),u_colorB*.16,.5+.5*sin(p.y*2.0));\n        col+=u_colorA*b1+u_colorC*b2+u_colorB*b3